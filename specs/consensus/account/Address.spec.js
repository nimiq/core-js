describe('AccountAddress', () => {
	const recipientAddr = new Address();
	const value = 1; 
	const fee = 1; 
	const nonce = 0;

	it('invariant to unserialize',() => {
		expect( ... ).toEquals( ... );
	});
});